# 酒店保洁产品进销存系统 — 技术改进路线图 (Roadmap)

本文档基于系统最新架构，整理并记录了未来待开发的技术升级与功能拓展规划。

---

## 1. 核心架构重构：状态管理与缓存 (State Management)

### 待改进任务：
- [ ] **引入 React Query (TanStack Query)**：
  - 使用 `useQuery` 管理产品、订单列表的获取，利用其内置的缓存、自动实时刷新和 Loading 状态。
  - 使用 `useMutation` 管理 CRUD 操作，确保在云端成功返回后再更新 UI。

---

## 2. 系统安全：权限规则防护 (Security Rules)

### 待改进任务：
- [ ] **配置云存储及数据库安全规则 (Security Rules)**：
  - 在 `cloudbaserc.json` 中定义细粒度规则：
    - `orders` / `finance_ledgers`：仅登录用户可读写，且禁止通过客户端直接修改已完成订单的关键核心数据。

---

## 3. 前端工程化：模块化与性能 (Frontend Refactoring)

### 待改进任务：
- [ ] **服务层拆分 (Service Pattern)**：
  - 拆分庞大的 `db.ts`，按照模块划分数据请求：
    ```bash
    src/services/
    ├── productService.ts    # 产品相关 API
    ├── orderService.ts      # 采购与销售单业务流
    ├── financeService.ts     # 财务管理接口
    └── userService.ts        # 成员与权限管理
    ```
- [ ] **Ant Design 组件优化**：
  - 优化大表格渲染性能，开启虚拟列表（Virtual List）应对上千条产品记录。
- [ ] **全局状态管理**：使用 `Zustand` 替代 `db.ts` 中的全局变量，存储当前用户信息和全局缓存（如 `userMap`）。

---

## 4. 监控与故障排查 (Observability)

### 待改进任务：
- [ ] **统一错误处理与请求拦截**：封装全局 `request` 工具，拦截云端数据库异常并向用户展示友好的中文错误提示。

---

## 5. 价格体系升级：多维度差异化定价 (Multi-Dimension Pricing)

系统目前只支持“一品一价”（单一成本价与销售价），无法满足按客户或发货仓库进行差异化报价的业务场景。

### 待改进任务：
- [ ] **方案 A：协议价/客户定价体系**
  - [ ] 新建数据库表 `customer_prices` 用于保存不同客户的商品专属折扣价。
  - [ ] 销售开单页面增加联动逻辑：选择客户并添加产品时，优先从协议价表拉取价格。
- [ ] **方案 B：多仓库定价与库存拆分体系**
  - [ ] 新建仓库基础信息表 `warehouses` 及仓库价格映射表 `warehouse_prices`。
  - [ ] 将原有的单一 `inventory` 表升级为“商品 + 仓库”联合主键的多仓库库存表。
  - [ ] 销售开单时选择出库仓库，自动根据该仓价格体系读取售价，并在出库时扣减对应仓库的库存。

---

## 6. 实施阶段规划

### 第一阶段 (P0 - 性能与体验)
1. 拆分 `db.ts` 到服务层。
2. 引入 React Query 重构数据获取流。

### 第二阶段 (P1 - 安全与容灾)
1. 配置腾讯云端数据库和存储的自定义安全规则，防止终端用户通过控制台（F12）越权直接修改敏感表。
2. 统一前端网络请求与全局错误拦截提示。

### 第三阶段 (P2 - 业务功能扩展)
1. 增加 ECharts 数据可视化图表看板（分析各酒店采购额及商品销量排行）。
2. 设计并开发多维度差异化价格体系（优先实现方案 A 客户协议价）。
3. 适配手机小程序端（实现仓管员手机扫码出入库与现场快速盘点）。

---

> **负责人：** Antigravity Coding Assistant
> **状态：** 计划中 (Draft)
> **日期：** 2026-06-03令式 `message` 和 `modal`。
  - 优化大表格性能，开启虚拟列表（Virtual List）应对上千条产品记录。
- [ ] **全局状态管理**：使用 `Zustand` 替代 `db.ts` 中的全局变量，存储当前用户信息和系统配置。

---

## 5. 监控与运维 (Observability)

**现状：** 缺乏错误日志，难以排查线上问题。

### 改进任务：
- [ ] **统一错误处理**：封装全局 `request` 工具，拦截云端异常并展示友好提示。
- [ ] **库存审计增强**：在 `inventoryLog` 中增加 `requestId`，可追溯单次操作关联的所有库存变动。

---

## 6. 实施优先级划分

### 第一阶段 (P0 - 关键修复)
1. 将 `confirmReceipt` 和 `confirmShipment` 迁移至云函数。
2. 修复 `db.ts` 的 ID 生成逻辑。
3. 配置基础安全规则。

### 第二阶段 (P1 - 体验与稳定性)
1. 引入 React Query 重构数据获取流。
2. 拆分 `db.ts` 到服务层。
3. 增加数据导出为 Excel 的服务端实现。

### 第三阶段 (P2 - 扩展功能)
1. 增加 ECharts 数据可视化看板。
2. 开发独立的小程序端（利用 CloudBase 一套后端）。
3. 设计并开发多维度差异化价格体系（包含客户协议价或分仓定价）。

---

## 7. 价格体系升级：多维度差异化定价 (Multi-Dimension Pricing)

**现状：** 系统目前只支持“一品一价”（单一成本价与销售价），无法满足按客户或发货仓库进行差异化报价的业务场景。

### 改进任务：
- [ ] **方案 A：协议价/客户定价体系**
  - [ ] 新建数据库表 `customer_prices` 用于保存不同客户的商品专属折扣价。
  - [ ] 销售开单页面增加联动逻辑：选择客户并添加产品时，优先从协议价表拉取价格。
- [ ] **方案 B：多仓库定价与库存拆分体系**
  - [ ] 新建仓库基础信息表 `warehouses` 及仓库价格映射表 `warehouse_prices`。
  - [ ] 将原有的单一 `inventory` 表升级为“商品 + 仓库”联合主键的多仓库库存表。
  - [ ] 销售开单时选择出库仓库，自动根据该仓价格体系读取售价，并在出库时扣减对应仓库的库存。

---

> **负责人：** Antigravity Coding Assistant
> **状态：** 计划中 (Draft)
> **日期：** 2026-06-03
