# 酒店保洁产品进销存系统 — 设计参考与规划文档 (v1.1)

> 本文档为项目的核心设计白皮书。汇总了当前的整体架构、数据模型、所有落地的功能模块，以及下一阶段的演进路线规划。

---

## 1. 项目概述

**业务场景**：面向酒店保洁产品销售领域的企业级进销存管理系统。
**核心理念**：以进销存（进货、销售、库存）为业务主轴，辅以极高标准的数据安全审计与防串溯源机制。
**当前版本**：v1.1（已完成基础闭环及企业级高级防呆设计）
**数据流向**：全云端无服务器架构，依赖云函数保障强数据一致性。

---

## 2. 技术栈架构

| 层级 | 技术选型 | 说明 |
|------|------|------|
| **前端基座** | React 18.x + Vite 8.x + TS | 极速响应的单页应用架构 |
| **UI 设计系统** | Ant Design 5.x | 定制化深色玻璃拟态（Glassmorphism）主题 |
| **持久层存储** | CloudBase MySQL | 腾讯云原生关系型数据库，保障数据ACID |
| **业务逻辑层** | Node.js Serverless (云函数) | 提供全局安全拦截、审计落库与高并发控制 |
| **报表与打印** | 原生 HTML+CSS Print | 脱离第三方依赖，高度定制化 A4 PDF 输出 |

---

## 3. 全局核心功能模块（v1.1 现状盘点）

目前系统已经百分之百覆盖了企业日常流转所需的 8 大核心模块：

### 3.1 数据监控与看盘 (Dashboard)
- **实时运营总览**：今日/本月销售额监控、待处理积压订单追踪。
- **智能预警引擎**：穿透全量商品，低于 `minStock` 的 SKU 自动进入红色警报列表。

### 3.2 档案中心 (Products, Customers, Suppliers)
- **多维商品库**：支持分类树、品牌、规格、计量单位，并可上传展示云端图片。
- **Excel 批量引擎**：支持使用 `.xlsx` 模板一键导入庞大的商品库与分类体系，内置防重去重机制。
- **客商档案**：供应商联络及财务信息维护；酒店客户信用及等级管理。

### 3.3 采购与销售全生命周期流转
- **单据流转状态机**：
  - `草稿`（允许自由编辑） → `已确认`（业务锁定） → `已入库/已发货`（库存联动）
- **A4 电子单据打印 (Print/PDF)**：内置浏览器级原生渲染方案，生成自带表头表尾、签字画押区及折扣信息的专业单据，支持一键保存为电子归档 PDF。
- **四维智能搜索引擎**：支持直接通过单据号（PO/SO）、外部流转号、发票号或客商名称秒级定位订单。

### 3.4 动态库存与手工盘点 (Inventory)
- **自动化库存涨跌**：完全解耦，由确认出库/入库动作底层触发，自动防止“超卖”。
- **全息时间轴溯源**：在库存列表点击任一商品，弹出历史明细图谱，追溯自建档以来的每一笔订单流入流出及手工盘点记录。

### 3.5 企业级安全防呆与防串溯源 (Security & Audit Logs)
- **全局操作审计记录**：在【系统设置】页，一切修改、新增、作废、盘点操作均被底层云函数“无死角截获”并入库。
- **高阶外键保护 (Soft-Delete)**：禁止物理删除产生过历史订单的商品，强制引导以“停用”替代“删除”，防止财务死账。
- **底层翻译与容错机制**：审计日志底层虽为防串记录了 UUID，但在前端通过数据智能比对自动翻译为如 `[SO202605180001]` 的直观业务编号。
- **全量快照备份**：支持将目前全系统数据（商品、交易、流水等）一键导出为 Excel 本地快照。

---

## 4. 统一代码与数据库规范

### 4.1 云端 API 与高并发防断流
- 所有涉及到多表（商品表+关联订单表）信息拉取的地方，强制采用 `串行 await` 或 `数据库事务`，以防御高并发查询导致的服务器 `ECONNRESET`（连接重置）雪崩。
- **返回体标准**：`{ code: 200, message: 'Success', data: [...] }`

### 4.2 订单防重号生成算法
- `业务前缀 (PO/SO)` + `年月日 (YYYYMMDD)` + `数据库自增4位流水号`。通过单独的 `counters` 序列表实现强互斥，杜绝并发重号。

---

## 5. 🚀 二期演进规划 (Phase 2 - 新功能计划清单)

在当前系统逻辑稳固、进销存数据链条严密闭环的基础上，建议在下一阶段（v2.0）启动以下新功能的研发以大幅提升人效和数字化深度：

### 🌟 高优推荐 (High Priority)

| 功能名称 | 业务价值 | 实现路径简述 |
|---------|---------|-------------|
| **1. 扫码枪与条码直读** | **效率颠覆**：仓库在出入库时，不需要再去界面里手敲搜索商品。只需拿普通扫码枪滴一下商品包装条码，屏幕自动带出商品并加 1 数量。 | 在商品库增加 `barcode` 字段。在全局采购/销售页监听键盘 `keydown` 事件（扫码枪本质是极速键盘输入）。 |
| **2. 角色权限分级 (RBAC)** | **安全管控**：酒店业务规模变大后，避免“人人都是超级管理员”。财务只能看报表，库管只能点发货不能改价格，销售只能录草稿单不能点审核。 | 建立 `Users` 和 `Roles` 表。在前端路由和后端云函数层分别加装 Token 拦截器，隐藏无权按钮。 |
| **3. 账期与应收应付管理** | **财务闭环**：当前只有简单的“已收款”，需要细化：此单还欠多少？月结客户本月总账单是多少？什么时候逾期警报？ | 新增 `finance_ledgers` 财务台账表，记录每一笔收付款时间及核销明细。 |

### 🛠 中优改进 (Medium Priority)

| 功能名称 | 业务价值 | 实现路径简述 |
|---------|---------|-------------|
| **4. 智能销量预测算法** | **降低资金占用**：利用过去 30 天/90 天的出库流速，结合当前库存，系统自动计算并提示：某品牌洗发水还能撑 7 天，建议马上补货 50 桶。 | 在看板新增“智能补货建议”模块，基于 `InventoryLog` 的历史斜率计算预估枯竭日。 |
| **5. ECharts 动态数据大屏** | **管理层洞察**：用酷炫的折线图、饼图直观展示年度销售趋势、各酒店客户带来的利润占比等。 | 引入 `echarts-for-react`，在 `/reports` 路由下绘制动态交互式分析图表。 |
| **6. 客户分级阶梯定价** | **销售策略**：对于 VVIP 大酒店，保洁剂拿货价自动按底价走；对散客酒店按指导价走。无需销售人员强行记忆或查表。 | 在 `customers` 表增加折扣系数或在 `products` 表扩展多个价格体系字段，制单时自动套用。 |

### 📱 长期战略 (Long-term Vision)

| 功能名称 | 业务价值 | 实现路径简述 |
|---------|---------|-------------|
| **7. 多仓库联动管理** | 解决酒店分区分楼层设小仓库、或多地发货调拨的问题，实现子母仓库库存划转。 | 在库存表增加 `warehouseId` 维度，新增内部调拨（Transfer Order）业务流。 |
| **8. 移动端/微信小程序适配** | 库管人员不需要再跑回电脑前操作，直接用手机在仓库现场点货、确认出库。 | 将现有 React 组件进行响应式重构，或抽离 API 提供给独立的小程序端使用。 |
